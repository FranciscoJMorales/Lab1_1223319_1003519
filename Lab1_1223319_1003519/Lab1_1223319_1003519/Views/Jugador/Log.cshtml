@model IEnumerable<Lab1_1223319_1003519.Models.Tiempo>

@{
    ViewBag.Title = "Log";
}

<h2>Resumen</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Log)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Log)
            </td>
        </tr>
    }

</table>
<p>
    @Html.ActionLink("Volver al inicio", "Index", "Jugador")
</p>